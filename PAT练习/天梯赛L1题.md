### **L1-072 刮刮彩票 (20 分)**

```c++
#include<iostream>
using namespace std;
int a[11][11];
//2021年4月12日17:39:42 
//注意输出数据的正确性
int  weight[1100] = {0,0,0,0,0,0,10000,36,720,360,80,252,108,72,54,180,72,180,119,36,306,1080,144,1800,3600};
int main()
{
	int x,y,sum = 0;
	for(int i = 1; i <= 3;i++)
	{
		for(int j = 1;j <= 3;j++)
		{
			scanf("%d",&a[i][j]);
			sum += a[i][j];
			if(a[i][j] == 0)
			{
				x = i, y = j;	
			}
		}
	}
	a[x][y] = (1+2+3+4+5+6+7+8+9) - sum;
	int w,e;
	for(int i = 0;i < 3;i++)
	{
		cin>>w>>e;
		cout<<a[w][e]<<endl;
	}

	int ans,total = 0;
	cin>>ans;
	if(ans >= 1 && ans <= 3)
	{
		for(int i = 1;i <= 3;i++)
			total += a[ans][i];
	}
	else if(ans > 3 && ans <= 6)
	{
		
		for(int i = 1;i <= 3;i++)
			total += a[i][ans-3];
	}
	else if(ans == 7) 
	{
		for(int i = 1;i <= 3;i++)
			total += a[i][i];
	}
	else if(ans == 8)
	{
		for(int i = 1;i <= 3;i++)
			total += a[i][3-i+1];
	}
	printf("%d",weight[total]);
	return 0;
}
```

### **L1-071 前世档案 (20 分)**

> 二叉树的性质

```c++
#include<iostream>
#include<string>
using namespace std;

int main()
{
	int n,m;
	cin>>n>>m;
	getchar();
	for(int i = 0;i < m;i++)
	{
		string  str;
		getline(cin,str);
		int cur_index = 1;
		for(int j = 0;j < str.size();j++)
		{
			if(str[j] == 'y')
			{
				cur_index = 2 * cur_index;
			}
			else 
			{
				cur_index = 2 * cur_index + 1;
			}
		}
		cout<<(cur_index - ((1<<n) - 1))<<endl;
	}
	return 0;
}
```

### **L1-070 吃火锅 (15 分)**

> 字符串，是否 包含子串

```c++
//2021年4月12日17:46:22
#include<iostream>
#include<string>

using namespace std;

int  main()
{
	string ans = "chi1 huo3 guo1",str;
	int cnt = 0,flag_index = -1,point_cnt = 0;
	while(getline(cin,str) && str != ".")
	{	
		for(int i = 0;i < str.size();i++)
		{
			int j = i;
			int cur = 0;
			while(j < str.size() && cur < ans.size() && str[j] == ans[cur])
				j++,cur++;
			if(cur == ans.size())
			{
				if(flag_index == -1)
					flag_index = cnt + 1;
				++point_cnt;
				break;
			}

		}

		++cnt;
	}
	cout<<cnt<<endl;
	if(point_cnt != 0)
		cout<<flag_index<<" "<<point_cnt<<endl;
	else
		cout<<"-_-#\n";
	return 0;
}
```

### **L1-069 胎压监测 (15 分)**

>

```c++
//2021年4月21日17:32:02
//2021年4月21日17:33:16
#include<iostream>
#include<cmath>

using namespace std;

int main()
{
    int a[5];
    int max_id = 0;
    for(int i = 1;i <= 4;i++)
   {     cin>>a[i];
        max_id = max(max_id,a[i]);
   }
    int t1,t2;
    cin>>t1>>t2;
    bool flag = true;
    int errorid = -1,errornum = 0;
    int cnt  = 0;
    for(int i = 1;i <= 4;i++)
    {
        if(abs(a[i] - max_id) <= t2 && a[i] >= t1)
        {
            cnt++;
        }
        else if(abs(a[i] - max_id) > t2 || a[i] < t1)
        {
            if(errorid == -1)
            {
                errorid = i;
              
            }
              ++errornum;
            flag = false;
        }
    }
    if(cnt == 4)
    {
        printf("Normal\n");
    }
    else if(errornum == 1)
    {
        printf("Warning: please check #%d!\n",errorid);
    }
    else if(errornum >= 2)
    {
        printf("Warning: please check all the tires!\n");
    }
    return 0;
}
```

### **L1-068 调和平均 (10 分)**

>

```c++
//2021年4月21日17:47:59
#include<iostream>
#include<cmath>

using namespace std;

int main()
{
    int n;
    cin>>n;
    double tmp,sum = 0;
    for(int i = 0;i < n;i++)
    {
        cin>>tmp;
        sum += 1/tmp;
    }
    sum = sum/n;
    sum = 1/sum;
    printf("%.2f\n",sum);
    return 0;
}
```

### **L1-067 洛希极限 (10 分)**

>注意：洛希极限是大天体半径的n倍,洛希极限为：n * r，距离与大天体半径的比值c：表明也是大天体半径的倍数。
>
>$$c * r > n * r \quad 距离c * r 大于 洛希极限 n * r时不会撕裂$$

```c++
//2021年4月21日17:51:49
#include<iostream>
#include<cmath>

using  namespace std;

int main()
{
    double a,c;
    int b;
    cin>>a>>b>>c;
    double s;
    if(b == 0)
    {
        //流体
        s = a * 2.455;


    }
    else 
    {
        s = a * 1.26;
    }
    printf("%.2f",s);
    if(s < c)
    {
        printf(" ^_^\n");
    }
    else 
    {
        printf(" T_T\n");
    }
    return 0;
}
```

### **L1-066 猫是液体 (5 分)**

>

```c++
// 2021年4月21日18:56:48
#include<iostream>

using namespace std;

int main()
{
    int a,b,c;
    cin>>a>>b>>c;
    cout<<a*b*c<<endl;
    return 0;
}
```

### **L1-065 嫑废话上代码 (5 分)**

>

```c++
// 2021年4月21日18:56:48
#include<iostream>

using namespace std;

int main()
{
    cout<<"Talk is cheap. Show me the code.\n";
    return 0;
}
```



###**L1-064 估值一亿的AI核心代码 (20 分)**

>`stringstream用于空格分割的字符串的切分`
>
>`给定一个字符串，你需要反转字符串中每个单词的字符顺序，同时仍保留空格和单词的初始顺序。
>示例 1:`
>
>```htm
>输入: "Let's take LeetCode contest"
>输出: "s'teL ekat edoCteeL tsetnoc" 
>```
>
>`注意：在字符串中，每个单词由单个空格分隔，并且字符串中不会有任何额外的空格。`
>
>```c++
>class Solution {
>public:
>    string reverseWords(string s) {
>        //使用C++的stringstream类和，string的reverse函数
>        if(s.size() == 0)
>            return "";
>        stringstream ss;	
>        ss << s;
>        string p,str;
>        while(ss >> p){
>            reverse(p.begin(), p.end()); //string 可以调用reverse函数,string的表现像一个vecor<char>
>            str = str + p + ' ';
>        }
>        string ans(str.begin(), str.begin()+str.size()-1);
>        return ans;
>    }
>};
>
>```
>
>

```c++
//2021年4月7日15:55:26
#include<iostream>
#include<sstream>
#include<cstring>

using namespace std;

int main()
{
  int n;
  cin>>n;
  getchar();

  string s;

  while (n--)
  {
    string str[1005];

    int cnt = 0;
    
    getline(cin,s);
    cout<<s<<endl<<"AI:";
    
    for(int i = 0;i < s.size();i++){
      if(isalnum(s[i])){
        if(s[i] != 'I')
        //把所有大写英文字母变成小写
          s[i] = tolower(s[i]);
        
      }
      else {
        s.insert(i," "); //对每个非数字和字母之前加空格
        ++i;
      }

      if(s[i] == '?')
        s[i] = '!';//把所有问号变成叹号
    }
    stringstream ss(s);//主要依靠这个函数来取消单词之前的空格
    while(ss>>s){
      str[cnt++] = s;
    }
     if(!isalnum(str[0][0]))
        cout<<" ";//第一个单词的第一个位置

    for(int i = 0;i < cnt;i++)
    {
        if(!isalnum(str[i][0])){
          //判断第一个字符是否为标点，标点前面不输出空格
          cout<<str[i];
        }
        else if((str[i] == "can" || str[i] == "could") && str[i + 1] == "you")
        {
          cout<<" I "<<str[i];//can you ,could you,对换为 i can,i could
          i++;
        }else if(str[i] == "I" || str[i] == "me"){
          cout<<" you";
        }
        else 
          cout<<" "<<str[i];
    }
        cout<<endl;
  }
  return 0;
}
```

### **L1-063 吃鱼还是吃肉 (10 分)**

>

```c++
//2021年4月21日19:40:22
#include<iostream>
#include<algorithm>

using namespace std;

int main()
{
    int n;
    cin>>n;
    for(int i = 0;i < n;i++)
    {
        int a,b,c;
        cin>>a>>b>>c;
        if(a == 1)
        {
            if(b < 130 )
            {
                printf("duo chi yu!");
            }
            else if(b == 130)
            {
                printf("wan mei!");
            }
            else if(b > 130)
            {
                printf("ni li hai!");
            }
            printf(" ");
            if(c < 27 )
            {
                printf("duo chi rou!");
            }
            else if(c == 27)
            {
                printf("wan mei!");
            }
            else if(c > 27)
            {
                printf("shao chi rou!");
            }
            cout<<endl;
        }
        else
        {
                             if(b < 129 )
                            {
                                printf("duo chi yu!");
                            }
                            else if(b == 129)
                            {
                                printf("wan mei!");
                            }
                            else if(b > 129)
                            {
                                printf("ni li hai!");
                            }
                            printf(" ");
                            if(c < 25 )
                            {
                                printf("duo chi rou!");
                            }
                            else if(c == 25)
                            {
                                printf("wan mei!");
                            }
                            else if(c > 25)
                            {
                                printf("shao chi rou!");
                            }
                            cout<<endl;
        }
    }
    return 0;
}
```

### **L1-062 幸运彩票 (15 分)**

>

```c++
//2021年4月21日19:48:22
#include<iostream>
#include<string>
using namespace std;

int main()
{
    int n;
    cin>>n;
    getchar();
    string str;
    for(int i = 0;i < n;i++)
    {
        getline(cin,str);
        int a = stoi(str.substr(0,3));
        int b = stoi(str.substr(3));
        int sum1 = 0;
        while(a)
        {
            sum1 += a%10;
            a /= 10;
        }

       int sum2 = 0;
       while (b)
       {
           sum2 += b%10;
           b /= 10;
       }
       if(sum1 == sum2)
       printf("You are lucky!\n");
       else 
        printf("Wish you good luck.\n");

    }
    return 0;
}
```

### **L1-061 新胖子公式 (10 分)**

>

```c++
//2021年4月21日19:52:24
#include<iostream>

using namespace std;

int  main()
{
    double a,b;
    cin>>a>>b;
    double sum = a/(b*b);
    printf("%.1f\n",sum);
    if(sum > 25)
    {
        printf("PANG");
    }
    else 
    {
        printf("Hai Xing");
    }
    return 0;
}
```

### **L1-060 心理阴影面积 (5 分)**

>

```c++
//2021年4月21日19:56:00
#include<iostream>

using namespace std;

int main()
{
    int x,y;
    cin>>x>>y;
    int s = (100 * 100 * 0.5) - (100 * y * 0.5) - (100-x)*100 * 0.5;
    printf("%d",s);
    return 0;
}
```

### **L1-059 敲笨钟 (20 分)**

>注意：`ong`的判断

```c++
//2021年4月21日19:57:58
#include<iostream>
#include<string>
using namespace std;

int main()
{
    int n;
    cin>>n;
    getchar();
    string str;
    for(int  i = 0;i < n;i++)
    {
        getline(cin,str);
        int cnt = 0;
        for(int k = 0;k < str.size();k++)
        {
            if((str[k] == 'o' && str[k + 1] == 'n' && str[k + 2] == 'g')&&(str[k + 3] == ',' || str[k + 3] == '.'))
            ++cnt;
        }
        if(cnt == 2)
        {
            int j,ans=0;
            for(j = str.size() - 1;j >= 0;j--)
            {
                if(str[j] == ' ')
                {
                    ++ans;
                    if(ans == 3)
                        break;
                }
            }
            string res = str.substr(0,j);
            res = res + " qiao ben zhong.";
            cout<<res<<endl;
            continue;
        }
        else
        {
            cout<<"Skipped\n";
        }
    }
    return 0;
}
```

### **L1-058 6翻了 (15 分)**

>

```c++
//2021年4月21日20:12:57
#include<iostream>
#include<string>
#include<sstream>


using namespace std;

int main()
{
    string str;
    getline(cin,str);
    for(int i = 0;i < str.size();)
    {
        if(str[i] != '6')
         {   cout<<str[i];
             i++;
         }
        else 
        {
             int j = i;
             while(j < str.size() && str[j] == '6')
                ++j;
            int cnt = j - i;
            if(cnt > 3 && cnt <= 9)
            {
                cout<<"9";
            }
            else if(cnt > 9)
            {
                cout<<"27";
            }
            else 
            {
                while(cnt--)printf("6");
            }
            i = j; 
        }
       
    }
    return 0;
}
```

### **L1-057 PTA使我精神焕发 (5 分)**

>

```c++
//2021年4月21日20:21:36
#include<iostream>

using namespace std;

int  main()
{
    cout<<"PTA shi3 wo3 jing1 shen2 huan4 fa1 !\n";
    return 0;
}
```

### **L1-056 猜数字 (20 分)**

>

```c++
//2021年4月21日20:22:44
#include<iostream>
#include<cmath>
#include<string>
#include<vector>
using namespace std;

int main()
{
    int n;
    cin>>n;
    double min_interval = 1<<30;
    string res;
    vector<pair<string,int> > list;
    double sum = 0;
    for(int i = 0;i < n;i++)
    {
        string id;
        int x;
        cin>>id>>x;
        sum += (double)x;
        list.push_back({id,x});
    }
    sum = sum/n * 0.5;

    for(auto &w : list)
    {
        if(abs(w.second - sum) < min_interval)
        {
            min_interval = abs(w.second - sum);
            res = w.first;
        }
    }
    printf("%d %s\n",(int)(sum),res.c_str());

    return 0;
}
```

### **L1-055 谁是赢家 (10 分)**

>

```c++
//2021年4月21日20:29:37
#include<iostream>
#include<cmath>

using namespace std;

int  main()
{
    int pa,pb,a = 0,b = 0;
    cin>>pa>>pb;
    for(int i = 0;i < 3;i++)
    {
        int tmp;
        cin>>tmp;
        if(tmp)
            ++b;
        else 
            ++a;
    }
    if(pa > pb && a >= 1 ||(pa < pb && a == 3))
    {
        printf("The winner is a: %d + %d\n",pa,a);
    }
    else 
    {
        printf("The winner is b: %d + %d\n",pb,b);
    }

    return 0;
}
```

### **L1-054 福到了 (15 分)**

>

```c++
//2021年4月21日20:33:08
#include<iostream>
#include<string>

using namespace std;

const int N = 110;

char M[N][N];

int main()
{
    char c;
    int n;
    cin>>c>>n;
    getchar();
    string str;
    for(int i = 0;i < n;i++)
    {
        getline(cin,str);
        for(int j = 0;j < str.size();j++)
        {
            M[i][j] = str[j];
        }
    }
    bool flag = true;
    for(int i = 0;i < n;i++)
    {
        for(int j = 0;j < n;j++)
        {
            if(M[i][j] != M[n - i - 1][n - j - 1])
            {
                flag = false;
                break;
            }
        }
    }
    if(flag)
    {
        cout<<"bu yong dao le\n";
        for(int  i = n - 1;i >= 0;i--)
        {
            for(int j = n - 1;j >= 0;j--)
            {
                if(M[i][j] != ' ')
                    M[i][j] = c;
                printf("%c",M[i][j]);
            }
            printf("\n");
        }
    }
    else  
    {
         for(int  i = n - 1;i >= 0;i--)
        {
            for(int j = n - 1;j >= 0;j--)
            {
                if(M[i][j] != ' ')
                    M[i][j] = c;
                printf("%c",M[i][j]);
            }
            printf("\n");
        }
    }
    return 0;
}
```

### **L1-053 电子汪 (10 分)**

>

```c++
//2021年4月21日21:39:21
#include<iostream>

using namespace std;

int main()
{
    int a,b;
    cin>>a>>b;
    for(int i = 0;i < a+b;i++)
        cout<<"Wang!";
    return 0;
}	
```

### **L1-052 2018我们要赢 (5 分)**

>

```c++
//2021年4月21日21:39:21
#include<iostream>

using namespace std;

int main()
{
    cout<<"2018\n";
    cout<<"wo3 men2 yao4 ying2 !\n";
    return 0;
}
```

### **L1-051 打折 (5 分)**

>

```c++
//2021年4月21日21:39:21
#include<iostream>

using namespace std;

int main()
{
    int a;
    double r;
    cin>>a>>r;
    double price = a * r * 0.1;
    printf("%.2f\n",price);
    return 0;
}
```

### **L1-050 倒数第N个字符串 (15 分)**

>26进制

```c++
//2021年4月21日21:39:21
#include<iostream>
#include<string>


using namespace std;

int main()
{
    int l,n;
    cin>>l>>n; 
    int sum = 1;
    for(int i = 0;i < l;i++)
    {
        sum = sum * 26;
    }
    sum = sum - n;
    string res = "";
    for(int i = 0;i < l;i++)
    {
        char c = 'a' + (sum%26);
        sum /= 26;
        res = c + res;
    }
    cout<<res<<endl;
    return 0;
}
```

### **L1-049 天梯赛座位分配 (20 分)**

>

```c++
//2021年4月21日21:52:25
#include<iostream>
#include<vector>
#include<unordered_map>
#include<cmath>

using namespace std;
const int N = 110;

int team[N];
int n;
int path[N][N][N];
unordered_map<int,int> vis;
int main()
{
    cin>>n;
    int maxsum = 0;
    for(int i = 1;i <= n;i++)
    {
        cin>>team[i];
        maxsum = max(team[i],maxsum);
    }
    //第一支队五
    int pre = 0,cur = 0;
    for(int i = 1;i <= maxsum;i++)
    {
        //每个队伍的十个人
        for(int j = 1;j <= 10;j++)
        {
            //每个学校
            for(int k = 1;k <= n;k++)
            {
                if(i <= team[k])
                {
                    if(pre == k)
                    {
                        cur += 2;
                     
                    }
                    else 
                    {
                        cur++;
                    
                    }
                    path[k][i][j] = cur;
                    pre = k;
                }
                
            }
        }
    }
    for(int i = 1;i <= n;i++)
    {
        cout<<"#"<<i<<endl;
        for(int j = 1;j <= team[i];j++)
        {
            for(int k = 1;k <= 10;k++)
            {
                //第j个队伍的第k个人
                printf("%d",path[i][j][k]);
                if(k != 10)
                    printf(" ");
                else 
                    printf("\n");
            }
        }
    }
 
    return 0;
}
```

>

```c++
#include<iostream>
#include<vector>
#include<algorithm>
#include<cstring>


using namespace std;
int main()
{
    int N;
    cin>>N;
    int s[110];
    int stream;
    for(int i = 0;i < N;i++)
    {
        cin>>stream;
        s[i] = stream * 10;
    }
    int k = 1;
    int num = 0;
    vector<vector<int> > stu;
    stu.resize(N);
    bool vis[110];
    memset(vis,false,sizeof(vis));
    while(num < N)
    {
        for(int i = 0;i < N;i++)
        {
            if((stu[i].size() < s[i]) && (!vis[i]))
            {
                stu[i].push_back(k);
                //如果没有只剩下一个队伍
                if(num != N - 1)
                    ++k;
                else 
                    k += 2;
            }

            if(stu[i].size() == s[i] && !vis[i])
            {
                vis[i] = true;
                num++;
            }
        }
    }
    for(int i = 0;i < N;i++)
    {
        cout<<"#"<<(i + 1)<<endl;
        int  x = 1;
        for(int  j = 0;j < stu[i].size();j++)
        {
            cout<<stu[i][j]<<(x % 10 ? " " : "\n");
            x++;
        }
    }
    return 0;
}
```

### **L1-048 矩阵A乘以B (15 分)**

>

```c++
//2021年4月21日22:49:38
#include<iostream>

using namespace std;

const  int N = 110;

int a[N][N],b[N][N],c[N][N];

int main()
{
    int t1,t2;
    cin>>t1>>t2;
    for(int  i = 1;i <= t1;i++)
    {
        for(int j = 1;j <= t2;j++)
        {
            cin>>a[i][j];
        }
    }
    int t3,t4;
    cin>>t3>>t4;
    for(int i = 1;i <= t3;i++)
    {
        for(int j = 1;j <= t4;j++)
        {
            cin>>b[i][j];
        }
    }

    if(t2 == t3)
    {
        cout<<t1<<" "<<t4<<endl;
        for(int i = 1;i <= t1;i++)
        {
            for(int j = 1;j <= t4;j++)
            {
                int sum = 0;
                for(int k = 1;k <= t2;k++)
                {
                    sum += a[i][k] * b[k][j];
                }
                c[i][j] = sum;
            }
        }
        for(int i = 1;i <= t1;i++)
        {
            for(int j = 1;j <= t4;j++)
            {
                cout<<c[i][j]<<(j != t4 ? " " : "\n");  
            }
        }
    }
    else 
    {
        printf("Error: %d != %d\n",t2,t3);
    }
    return 0;
}
```

### **L1-047 装睡 (10 分)**

>

```c++
//2021年4月21日22:56:47
#include<iostream>
#include<string>

using namespace std;

int main()
{
    int n;
    cin>>n;
    for(int i = 0;i < n;i++)
    {
        string a;
        int b,c;
        cin>>a>>b>>c;
        if(b < 15 || b > 20 || c < 50 || c > 70)
        {
            cout<<a<<endl;
        }
    }
    return 0;
}
```

### **L1-046 整除光棍 (20 分)**

```c++
//2021年4月22日13:37:24
#include<iostream>
#include<string>

using namespace std;

int main()
{
    string str = "1";
    int res = 1;
    int x;
    cin>>x;
    string cur;
    while(true)
    {
        cur = "";
        int pre = 0;
        for(int i = 0;i < str.size();i++)
        {
            pre = pre * 10 + str[i] - '0';
            cur.push_back((pre/x + '0'));
            pre = pre%x;
        }
        if(pre == 0)
            break;
        res++;
        str.push_back('1');
    }

    int i = 0;
    while(i < cur.size() && cur[i] == '0')
        ++i;
    string ans = cur.substr(i);
    cout<<ans<<" "<<res<<endl;

    return 0;
}
```

> 模拟整数除法

```c++
//2021年4月23日09:08:00
#include<iostream>
#include<cmath>

using namespace std;

int main()
{
    int x;
    cin>>x;
    int s = 1,cnt = 1;
    while(s < x)
    {
        s = s * 10 + 1;
        cnt++;
    }
    while (true)
    {
        printf("%d",s/x);
        if((s%x) == 0)
            break;
        s = (s % x) * 10 + 1;
        ++cnt;
    }
    printf(" %d",cnt);
    
    return 0;
}
```



### **L1-045 宇宙无敌大招呼 (5 分)**

>

```c++
//2021年4月22日13:37:24
#include<iostream>
#include<string>

using namespace std;

int main()
{
    string str;
    getline(cin,str);
    cout<<"Hello "<<str<<endl;

    return 0;
}
```

### **L1-044 稳赢 (15 分)**

>

```c++
//2021年4月22日13:37:24
#include<iostream>
#include<string>

using namespace std;

int main()
{
    int k;
    cin>>k;
    getchar();
    string str;
    int l = 0;
    while(getline(cin,str) && str != "End")
    {
       
        l++;
        if(l % (k + 1) == 0)
        {
            cout<<str<<endl;
        }
        else  
        {
            if(str == "ChuiZi")
            {
                cout<<"Bu\n";
            }
            else if(str ==  "Bu")
            {
                cout<<"JianDao\n";
            }
            else
            {
                cout<<"ChuiZi\n";
            }
        }

    }


    return 0;
}
```

### **L1-043 阅览室 (20 分)**

>

```c++
//2021年4月22日14:01:01
#include<iostream>
#include<cstring>

using namespace std;
const int N = 1005;

char book_status[N];

int last_time[N];
int main()
{
    int n;
    scanf("%d",&n);
    for(int i = 0;i < n;i++)
    {
        memset(book_status,-1,sizeof(book_status));
        int total = 0,cnt = 0;
        int hh,mm,id;
        char c;
        while(true)
        {
            scanf("%d %c %d:%d",&id,&c,&hh,&mm);

            if(id == 0)
            {
                if(cnt == 0)
                {
                    printf("0 0\n");
                }
                else 
                {
                    double sum = total/(cnt * 1.0) + 0.5;
                    printf("%d %d\n",cnt,(int)sum);
                }
                break;
            }

            if(c == 'S')
            {
                book_status[id] = c;
                last_time[id] = hh * 60 + mm;

            }
            else if(c == 'E' && book_status[id] == 'S')
            {
                total += hh * 60 +  mm - last_time[id];
                last_time[id] = 0;
                ++cnt;
                book_status[id] = c;
            }
        }
    }
    return 0;
}
```

### **L1-042 日期格式化 (5 分)**

>

```c++
#include<iostream>
#include<cstring>
//2021年4月22日14:03:04
using namespace std;
const int N = 1005;

char book_status[N];

int last_time[N];
int main()
{
    int dd,mm,yy;
    scanf("%d-%d-%d",&mm,&dd,&yy);
    printf("%02d-%02d-%02d",yy,mm,dd);
    return 0;
}
```

### **L1-041 寻找250 (10 分)**

>

```c++
#include<iostream>
#include<cstring>
#include<sstream>
#include<string>
//2021年4月22日14:03:50
using namespace std;

int main()
{
    string str;
    getline(cin,str);
    stringstream ss(str);
    int cnt = 0;
    while(ss>>str)
    {
        ++cnt;
        if(str == "250")
        {
            break;
        }
    }
    printf("%d",cnt);
   
    return 0;
}
```

### **L1-040 最佳情侣身高差 (10 分)**

>

```c++
#include<iostream>
#include<cstring>
#include<sstream>
#include<string>
//2021年4月22日14:06:24
using namespace std;

int main()
{
    int n;
    cin>>n;
    for(int i = 0;i < n;i++)
    {
        char c;
        double x;
        cin>>c>>x;
        if(c == 'M')
        {
            printf("%.2f\n",x/1.09);
        }
        else   
        {
            printf("%.2f\n",x * 1.09);
        }
    }
   
    return 0;
}
```

### **L1-039 古风排版 (20 分)**

>

```c++
#include<iostream>
#include<cstring>
#include<sstream>
#include<string>
//2021年4月22日14:06:24
using namespace std;
const int N = 110;
char g[N][N];
int main()
{
    int n;
    cin>>n;
    string str;
    getchar();
    getline(cin,str);
    int cur = 0;
    int len = str.size() / n;
    if(str.size() % n)
        len++;
    for(int  i = 0;i < len;i++)
    {
        for(int j = 0;j < n;j++)
        {
            if(cur < str.size())
                g[i][j] = str[cur++];
            else 
                g[i][j] = ' ';
        }
    }
    for(int i = 0;i < n;i++)
    {
        for(int j = len - 1;j >= 0;j--)
        {
            printf("%c",g[j][i]);
        }
        printf("\n");
    }
    return 0;
}
```

### **L1-038 新世界 (5 分)**

>

```c++
#include<iostream>
#include<cstring>
#include<sstream>
#include<string>
//2021年4月22日14:06:24
using namespace std;
const int N = 110;
char g[N][N];
int main()
{
    printf("Hello World\n");
    printf("Hello New World\n");
    return 0;
}
```

### **L1-037 A除以B (10 分)**

>

```c++
#include<iostream>
#include<cstring>
#include<sstream>
#include<string>
//2021年4月22日14:06:24
using namespace std;
const int N = 110;
char g[N][N];
int main()
{
    int a,b;
    cin>>a>>b;
    printf("%d/",a);
    if(b > 0)
    {
        printf("%d=%.2f\n",b,a/(1.0*b));
    }
    else if(b == 0)
    {
        printf("%d=Error\n",b);
    }
    else 
    {
        printf("(%d)=%.2f\n",b,a/(b*1.0));
    }
    return 0;
}
```

### **L1-036 A乘以B (5 分)**

>

```c++
#include<iostream>
#include<cstring>
#include<sstream>
#include<string>
//2021年4月22日14:06:24
using namespace std;
const int N = 110;
char g[N][N];
int main()
{
    int a,b;
    cin>>a>>b;
    printf("%d\n",a*b);
    return 0;
}
```

### **L1-035 情人节 (15 分)**

>

```c++
#include<iostream>
#include<cstring>
#include<sstream>
#include<string>
//2021年4月22日14:06:24
using namespace std;
const int N = 110;
char g[N][N];
int main()
{
    string str;
    
    int cur = 0;
    string t1,t2;
    while(getline(cin,str) && str != ".")
    {
        ++cur;
        if(cur == 2)
        {
            t1 = str;
        }
        if(cur == 14)
        {
            t2 = str;
        }
    }
    if(cur < 2)
    {
        printf("Momo... No one is for you ...\n");
    }
    else if(cur < 14)
    {
        printf("%s is the only one for you...\n",t1.c_str());
    }
    else 
    {
        printf("%s and %s are inviting you to dinner...\n",t1.c_str(),t2.c_str());
    }
    return 0;
}
```

### **L1-034 点赞 (20 分)**

>

```c++
//2021年4月22日14:26:04
#include<iostream>
#include<string>
#include<unordered_map>
#include<cmath>

using namespace std;

int  main()
{
    int n;
    cin>>n;
    unordered_map<int,int> list;
    int first_id,max_num = 0;
    for(int i = 0;i < n;i++)
    {
        int k;
        cin>>k;
        for(int j = 0;j < k;j++)
        {
            int tmp;
            cin>>tmp;
            list[tmp]++;
            if(list[tmp] > max_num)
            {
                max_num = list[tmp];
                first_id = tmp;
            }
            else if(list[tmp] == max_num)
            {
                if(first_id < tmp)
                first_id = tmp;
            }
        }
    }
    cout<<first_id<<" "<<max_num<<endl;
    return 0;
}
```

### **L1-033 出生年 (15 分)**

>

```c++
//2021年4月22日14:26:04
#include<iostream>
#include<string>
#include<unordered_map>
#include<cmath>
#include<set>
using namespace std;

int  main()
{
    string y;
    int n,cnt = 0;
    cin>>y>>n;
    while(true)
    {
        if(y.size() < 4)
            y.insert(0,4-y.size(),'0');
        set<char> st;
        for(int j = 0;j < y.size();j++)
            st.insert(y[j]);
        if(st.size() == n)
        {
            break;
        }
        y = to_string(stoi(y) +1);
        cnt++;
    }
    if(y.size() < 4)
        y.insert(0,4 - y.size(),'0');
    cout<<cnt<<" "<<y<<endl;

    return 0;
}
```

### **L1-032 Left-pad (20 分)**

>

```c++
//2021年4月22日14:26:04
#include<iostream>
#include<string>
#include<unordered_map>
#include<cmath>
#include<set>
using namespace std;

int  main()
{
    int n;
    char c;
    cin>>n>>c;
    getchar();
    string str;
    getline(cin,str);
    if(str.size() < n)
        str.insert(0,n - str.size(),c);
    for(int i = str.size() - n;i < str.size();i++)
        printf("%c",str[i]);

    return 0;
}
```

### **L1-031 到底是不是太胖了 (10 分)**

>

```c++
	//2021年4月22日14:39:19
#include<iostream>
#include<cmath>

using namespace std;

int main()
{
    int n;
    cin>>n;
    for(int i = 0;i < n;i++)
    {
        double a,b;
        cin>>a>>b;
        double statardw = (a - 100) * 0.9 * 2;
        if(abs(b - statardw) < statardw * 0.1)
        {
            printf("You are wan mei!\n");
        }
        else if(statardw < b)
        {
            printf("You are tai pang le!\n");
        }
        else 
        {
            printf("You are tai shou le!\n");
        }
    }
    return 0;
}
```

### **L1-030 一帮一 (15 分)**

>

```c++
//2021年4月22日14:43:57
#include<iostream>
#include<string>
#include<vector>
#include<unordered_map>

using namespace std;
unordered_map<string,int> vis;
int main()
{
    int n;
    cin>>n;
    vector<pair<string,int> > res;
    for(int i = 0;i < n;i++)
    {
        int flag;
        string id;
        cin>>flag>>id;
        res.push_back({id,flag});
    }
    
    for(int i = 0;i < res.size()/2;i++)
    {
        if(!vis.count(res[i].first))
        {
            printf("%s ",res[i].first.c_str());
            vis[res[i].first] = 1;

            int f = res[i].second;
   
            for(int  j = res.size() - 1;j >= 0;j--)
            {
                if(!vis.count(res[j].first) && res[j].second != f)
                {
                    printf("%s\n",res[j].first.c_str());
                    vis[res[j].first] = 1;
                    break;
                }
            }
        }
        
    }

    return 0;
}
```

### **L1-029 是不是太胖了 (5 分)**

>

```c++
//2021年4月22日14:50:47
#include<iostream>

using namespace std;

int main()
{
    double a;
    cin>>a;
    double sum = (a - 100) * 0.9 * 2;
    printf("%.1f\n",sum);
    return 0;
}
```

### **L1-028 判断素数 (10 分)**

>

```c++
//2021年4月22日14:52:14
#include<iostream>
#include<cstring>
#include<cmath>

using namespace std;

const int  N = 10005;

bool isprime(int x)
{
    if(x == 0 || x == 1)
        return false;
    for(int i = 2;i <= sqrt(x);i++)
        if(x % i == 0)
            return false;
        return true;
}
int main()
{
    int n;
    cin>>n;
    for(int i = 0;i < n;i++)
    {
        int tmp;
        cin>>tmp;
        cout<<(isprime(tmp) ? "Yes\n" : "No\n");
    }
    return 0;
}
```

### **L1-027 出租 (20 分)**

>

```c++
//2021年4月22日14:55:09
#include<iostream>
#include<set>
#include<vector>
#include<string>
#include<algorithm>


using namespace std;
const int N = 14;
int g[N];

bool cmp(int a,int b)
{
    return  a > b;
}
int main()
{
    string str;
    getline(cin,str);
    vector<int> arr;
    set<int> st;
    for(int i = 0;i < str.size();i++)
    {
        int x = str[i] - '0';
        if(!st.count(x))
        {
            arr.push_back(x);
            st.insert(x);
        }
    }
    sort(arr.begin(),arr.end(),cmp);
    
    printf("int[] arr = new int[]{");

    for(int j = 0;j < arr.size();j++)
    {
        printf("%d",arr[j]);
        g[arr[j]] = j;
        if(j != arr.size() - 1)
            printf(",");
        else 
            printf("};\n");
    }
    printf("int[] index = new int[]{");
    for(int i = 0;i < str.size();i++)
    {
        int x = str[i] - '0';

        printf("%d",g[x]);
         if(i != str.size() - 1)
            printf(",");
        else 
            printf("};\n");
    }

    return 0;
}
```

### **L1-026 I Love GPLT (5 分)**

>

```c++
//2021年4月22日15:04:57
#include<iostream>
#include<string>

using namespace std;

int main()
{
    string str = "I Love GPLT";
    for(int i = 0;i < str.size();i++)
    {
        cout<<str[i]<<endl;
    }
    return 0;
}
```

###L1-025 正整数A+B (15 分)

>

```c++
///2021年4月22日15:06:31
#include<iostream>
#include<string>

using namespace std;
int judge(string &s)
{
    int  sum = 0;
    for(int i = 0;i < s.size();i++)
    {
        if(!isdigit(s[i]))
            return -1;
        else 
            sum = sum * 10 + s[i] - '0';
    }
    if(sum < 1 || sum > 1000)
        return -1;
    return sum;
}
int main()
{
    string str;
    getline(cin,str);
    int x = str.find(' ');
    string a = str.substr(0,x);
    string b = str.substr(x + 1);
    if(judge(a) != -1)
    {
        cout<<a;
    }
    else 
    {
        cout<<"?";
    }
    cout<<" + ";
    if(judge(b) != -1)
    {
        cout<<b;
    }
    else 
    {
        cout<<"?";
    }
    cout<<" = ";
    if(judge(a) != -1 && judge(b) != -1)
    {
        cout<<stoi(a) + stoi(b);
    }
    else 
    {
        cout<<"?";
    }

    return 0;
}
```



### **L1-024 后天 (5 分)**

>

```c++
//2021年4月22日15:11:40
#include<iostream>

using namespace std;

int main()
{
    int d;
    cin>>d;
    d = (d + 2) % 7;
    if(d == 0)
        d = 7;
    cout<<d<<endl;
    return 0;
}
```

### **L1-023 输出GPLT (20 分)**

>

```c++
//2021年4月22日15:13:02
#include<iostream>
#include<string>
#include<unordered_map>

using  namespace std;

int main()
{
    string str;
    getline(cin,str);
    unordered_map<char,int> cnt;
    for(int i = 0;i < str.size();i++)
    {
        char c = toupper(str[i]);
        cnt[c]++;
    }
    while(cnt['P'] || cnt['G'] || cnt['L']||cnt['T'])
    {
        if(cnt['G'])
        {
            cout<<'G';
            cnt['G']--;
        }
         if(cnt['P'])
        {
            cout<<'P';
            cnt['P']--;
        }
        
         if(cnt['L'])
        {
            cout<<'L';
            cnt['L']--;
        }
        
         if(cnt['T'])
        {
            cout<<'T';
            cnt['T']--;
        }
        
    }
    return 0;
}
```

### **L1-022 奇偶分家 (10 分)**

>

```c++
//2021年4月22日15:17:11
#include<iostream>

using namespace std;

int main()
{
    int n;
    cin>>n;
    int tmp;
    int even = 0,odd = 0;
    for(int i = 0;i < n;i++)
    {
        cin>>tmp;
        if(tmp % 2 == 0)
        {
            even++;
        }
        else 
        {
            odd++;
        }
    }
    printf("%d %d\n",odd,even);
    return 0;
}
```

### **L1-021 重要的话说三遍 (5 分)**

>

```c++
//2021年4月22日15:19:36
#include<iostream>

using namespace std;

int main()
{
    for(int i = 0;i < 3;i++)
    {
        cout<<"I'm gonna WIN!\n";
    }
    return 0;
}
```

### **L1-020 帅到没朋友 (20 分)**

>

```c++
//2021年4月22日15:21:26
#include<iostream>
#include<unordered_map>
#include<vector>

using namespace std;

int main()
{
    int n;
    cin>>n;
    unordered_map<int,int> vis;
    for(int i = 0;i < n;i++)
    {
        int k;
        cin>>k;
        for(int j = 0;j < k;j++)
        {
            int tmp;
            cin>>tmp;
            if(vis[tmp] < k)
                vis[tmp] = k;
        }
    }
    int m;
    cin>>m;
    vector<int> ans;
    unordered_map<int,int> pre;
    for(int i = 0 ;i < m;i++)
    {
        int tmp;
        cin>>tmp;
        if(vis[tmp] <= 1 && !pre.count(tmp))
        {
            ans.push_back(tmp);
            pre[tmp] = 1;
        }
    }
    if(ans.size())
    {
        for(int i = 0;i < ans.size();i++)
        {
            printf("%05d%s",ans[i],(i != ans.size() - 1 ? " " : "\n"));

        }
    }
    else 
    {
        printf("No one is handsome\n");
    }
    return 0;
}
```

### **L1-019 谁先倒 (15 分)**

>

```c++
//2021年4月22日15:27:27
#include<iostream>
#include<algorithm>

using namespace std;

int main()
{
    int pa,pb;
    cin>>pa>>pb;
    int n;
    int t1 = 0,t2 = 0;
    cin>>n;
    for(int i = 0;i < n;i++)
    {
        int a,b,c,d;
        cin>>a>>b>>c>>d;
        int sum = a + c;
        if(b == sum && d != sum)
        {
            t1++;
            if(t1 == pa + 1)
            {
                cout<<'A'<<endl;
                cout<<t2<<endl;
                return 0;
            }
        }
        else if(b != sum && d == sum)
        {
            t2++;
            if(t2 == pb + 1)
            {
                cout<<'B'<<endl;
                cout<<t1<<endl;
                return 0;
            }
        }
    }
    return 0;
}
```

###L1-018 大笨钟 (10 分)

>
>
>

```c++
//2021年4月22日15:33:39
#include<iostream>
#include<string>

using namespace std;

int main()
{
    int hh,mm;
    scanf("%d:%d",&hh,&mm);
    if(hh < 12 || (hh == 12 && mm == 0))
    {
        printf("Only %02d:%02d.  Too early to Dang.\n",hh,mm);
    }
    else 
    {
        int len = hh - 12;
        if(mm)
            len++;
        while(len--)
            printf("Dang");
    }
    return 0;
}
```

### **L1-017 到底有多二 (15 分)**

>

```c++
//2021年4月22日15:36:23
#include<iostream>
#include<string>

using namespace std;

int main()
{
    string str;
    getline(cin,str);
    double a = 1,b = 1;
    string res = str;
    if(str[0] == '-')
    {
        a += 0.5;
        res = str.substr(1);
    }
    if((res.back() - '0') % 2 == 0)
        b += 1;
    int cnt = 0;
    for(int i = 0;i < res.size();i++)
    {
        if(res[i] == '2')
            ++cnt;
    }
    double sum = cnt/(res.size() * 1.0) * a * b;
    printf("%.2f%%\n",sum * 100);
    return 0;
}
```

### **L1-016 查验身份证 (15 分)**

>

```c++
//2021年4月22日15:40:38
#include<iostream>
#include<string>

using namespace std;
char M[11] = {'1','0','X','9','8','7','6','5','4','3','2'};
int weight[17] = {7,9,10,5,8,4,2,1,6,3,7,9,10,5,8,4,2};
bool judge(string s)
{
    int sum = 0;
    for(int i = 0;i < 17;i++)
    {
        if(!isdigit(s[i]))
            return false;
        else 
            sum += (s[i] - '0') * weight[i];
    }
    return s.back() == M[sum%11];
}
int main()
{
    int n;
    cin>>n;
    getchar();
    string str;
    int ans = 0;
    for(int  i = 0;i < n;i++)
    {
        getline(cin,str);
        if(!judge(str))
        {
            ++ans;
            cout<<str<<endl;
        }
    }
    if(ans == 0)
    {
        printf("All passed\n");
    }
    return 0;
}
```

### **L1-015 跟奥巴马一起画方块 (15 分)**

>

```c++
//2021年4月22日15:46:11
#include<iostream>
#include<string>

using namespace std;

int main()
{
    int n;
    char c;
    cin>>n>>c;
    int row = (int)(n * 0.5 + 0.5);
    for(int  i = 0;i < row;i++)
    {
        for(int  j = 0;j < n;j++)
            cout<<c;
        cout<<endl;
    }
    return 0;
}
```

### **L1-014 简单题 (5 分)**

>

```c++
//2021年4月22日15:48:29
#include<iostream>

using namespace std;

int main()
{
    cout<<"This is a simple problem.\n";
    return 0;
}
```

### **L1-013 计算阶乘和 (10 分)**

>

```c++
///2021年4月22日16:28:24
#include<iostream>

using namespace std;

int dp[15];

int main()
{
    dp[1] = 1;
    int n;
    cin>>n;
    int sum  = 0;
    for(int i = 1;i <= n;i++)
    {
        sum += dp[i];
        dp[i + 1] = dp[i] * (i + 1);
    }
    cout<<sum<<endl;
    return  0;
}
```

### **L1-012 计算指数 (5 分)**

>

```c++
//2021年4月22日16:31:38
#include<iostream>

using namespace std;

int main()
{
    int n;
    cin>>n;
    int sum  = 1<<n;
    printf("2^%d = %d\n",n,sum);
    return 0;
}
```

### **L1-011 A-B (20 分)**

>

```c++
///2021年4月22日16:33:55
#include<iostream>
#include<string>
#include<unordered_map>

using namespace std;

unordered_map<char,int> vis;
int main()
{
    string a,b;
    getline(cin,a);
    getline(cin,b);
    for(int i = 0;i < b.size();i++)
    {
        vis[b[i]] = 1;
    }
    for(int i = 0;i < a.size();i++)
    {
        if(!vis.count(a[i]))
        {
            cout<<a[i];
        }
    }
    return 0;
}
```

### **L1-010 比较大小 (10 分)**

>

```c++
//2021年4月22日16:36:00
#include<iostream>
#include<cmath>

using namespace std;

int main()
{
    int a,b,c;
    cin>>a>>b>>c;
    if(a > b)
        swap(a,b);
    if(b > c)
        swap(b,c);
    if(a > b)
        swap(a,b);
    cout<<a<<"->"<<b<<"->"<<c;
    return 0;
}
```

### **L1-009 N个数求和 (20 分)**

>

```c++
//2021年4月22日16:38:28
#include<iostream>
#include<cmath>

using namespace std;
typedef long long ll;
int n;
ll gcd(ll a,ll b)
{
    return b == 0 ? a : gcd(b,a%b);
}
int main()
{
    cin>>n;
    int sum1 = 0,sum2 = 1;
    for(int i = 0;i < n;i++)
    {
        ll a,b;
        scanf("%lld/%lld",&a,&b);
        ll t = gcd(abs(a),abs(b));
        a /= t;
        b /= t;
        sum1 = sum1 * b + sum2 * a;
        sum2 = sum2 * b;

        t = gcd(abs(sum1),abs(sum2));
        sum1 /= t;
        sum2 /= t;
    }
    if(sum1 < 0)
        printf("-");
    if(sum1 / sum2)
    {
        printf("%lld",abs(sum1)/abs(sum2));
    }
    if(sum1/sum2 && sum1%sum2)
    {
        printf(" ");
    }
    if(sum1%sum2)
    {
        printf("%lld/%lld\n",abs(sum1%sum2),abs(sum2));
    }
    if(sum1 == 0)
    {
        printf("0");
    }
    return 0;
}
```

### **L1-008 求整数段和 (10 分)**

>

```c++
//2021年4月22日16:47:03
#include<iostream>
#include<cmath>

using namespace std;

int main()
{
    int a,b;
    int sum = 0;
    cin>>a>>b;
    int cnt = 0;
    for(int i = a;i <= b;i++)
    {
        printf("%5d",i);
        sum += i;
        cnt++;
        if(cnt % 5 == 0 || i == b)
            printf("\n");
        
    }
    cout<<"Sum = "<<sum<<endl;
    return 0;
}
```

### **L1-007 念数字 (10 分)**

>

```c++
//2021年4月22日16:50:01
#include<iostream>

#include<string>

using namespace std;

int main()
{
    string res[10] = {"ling","yi","er","san","si","wu","liu","qi","ba","jiu"};
    string s;
    getline(cin,s);
    for(int i = 0;i < s.size();i++)
    {
        if(s[i] == '-')
            cout<<"fu";
        else 
            cout<<res[(s[i] - '0')];
        if(i != s.size() - 1)
            cout<<" ";
        else 
            cout<<endl;
    }
    return 0;
}
```

### **L1-006 连续因子 (20 分)**

>

```c++
    #include<iostream>
    #include<cmath>
    using namespace std;

    int main()
    {
        int n;
        cin>>n;
        int resultnum = 0,flag = 0;
        for(int i = 2;i <= sqrt(n);i++)
        {
            if(n % i == 0)
            {
                int tmp = n,cur  = i;
                while(tmp%cur == 0 && tmp)
                {
                    tmp /= cur;
                    ++cur;
                }
                if(n % tmp == 0)
                if(cur - i > resultnum)
                {
                    resultnum = cur - i;
                    flag = i;
                }
            }
            
        }

        if(flag)
        {
            cout<<resultnum<<endl;
        for(int i = 0;i < resultnum;i++)
        {
            if(i != 0)
                printf("*");
            printf("%d",i + flag);
        }
        }
        else 
        {
            cout<<"1\n";
            cout<<n<<endl;
        }
        
        
        return 0;
    }
```

> 枚举：长度len

```c++
//2021年4月23日09:03:10
#include<iostream>
#include<cmath>

using namespace std;
typedef long long ll;
int main()
{
    int n;
    cin>>n;
    ll ans;
    int maxn = sqrt(n);

    for(int len = 13;len >= 1;len--)
    {
        for(int start = 2;start <= maxn;start++)
        {
            ans = 1;
            for(int j = start;j - start < len;j++)
                ans *= j;
            if(n % ans == 0)
            {
                printf("%d\n%d",len,start);
                for(int j = 1;j < len;j++)
                    printf("*%d",j + start);
                return 0;
            }
        }
    }
    printf("1\n%d",n);

}
```



### **L1-005 考试座位号 (15 分)**

>

```c++
//2021年4月22日17:17:32
#include<iostream>
#include<unordered_map>
#include<string>
using namespace std;

using pii = pair<string,int>;
unordered_map<int,pii> list;
int main()
{
    int n;
    cin>>n;
    for(int i = 0;i < n;i++)
    {
        string id;
        int a,b;
        cin>>id>>a>>b;
        list[a].first = id;
        list[a].second = b;
    }
    int m;
    cin>>m;
    for(int i = 0;i < m;i++)
    {
        int id;
        cin>>id;
        cout<<list[id].first<<" "<<list[id].second<<endl;
    }
    return 0;
}
```

### **L1-004 计算摄氏温度 (5 分)**

>

```c++
//2021年4月22日17:20:47
#include<iostream>

using namespace std;

int main()
{
    int f;
    cin>>f;
    cout<<"Celsius = ";
    cout<<5*(f - 32)/9<<endl;
    return 0;
}
```

### **L1-003 个位数统计 (15 分)**

>

```c++
//2021年4月22日17:22:37
#include<iostream>
#include<map>
#include<string>

using namespace std;

map<int,int> mp;
int main()
{
    string str;
    getline(cin,str);
    for(int i = 0;i < str.size();i++)
    {
        int x = str[i] - '0';
        mp[x]++;
    }
    map<int,int>::iterator i;
    for(i = mp.begin();i != mp.end();i++)
        cout<<i->first<<":"<<i->second<<endl;

    return 0;
}
```

### **L1-002 打印沙漏 (20 分)**

>

```c++
//2021年4月22日17:25:06
#include<iostream>
#include<cmath>

using namespace std;

int  main()
{
    int cnt;
    char c;
    cin>>cnt>>c;
    if(cnt < 7)
    {
        cout<<c<<endl;
        cout<<cnt-1<<endl;
    }
    else 
    {
        int cur = 1;
        int i = 1;
        while(cur <= cnt)
        {
            i++;
            cur += 2 * (2 * i - 1);
        }
        if(cur > cnt)
            cur -= 2*(2*i - 1);

        for(int j = i - 1;j >= 1;j--)
        {
            for(int d = 1;d < i - j;d++)
                printf(" ");
            for(int d = 0;d < 2 * j - 1;d++)
                printf("%c",c);
            printf("\n");
        }
        for(int j = 2;j < i;j++)
        {
            for(int d = 1;d < i - j;d++)
                printf(" ");
            for(int d = 0;d < 2 * j - 1;d++)
                printf("%c",c);
            printf("\n");
        }
        cout<<cnt-cur<<endl;
    }
    return 0;
}
```

### **L1-001 Hello World (5 分)**

>

```c++
//2021年4月22日17:34:24
#include<iostream>

using namespace std;

int main()
{
    cout<<"Hello World!\n";
    return 0;
}
```

